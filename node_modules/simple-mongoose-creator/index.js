'use strict'

const mongoose = require('mongoose');

exports.smc = (collection_name, object_schema) => {
    const schema = new mongoose.Schema(object_schema);
    return mongoose.model(collection_name, schema);
}
